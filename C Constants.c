#include <stdio.h>

int main() {
    const int birthYear = 2000; // Constant variable
    printf("Birth Year: %d\n", birthYear);

    // birthYear = 2001; // This line will give an error if uncommented (cannot change a constant)

    return 0;
}
